export type UsuarioComunidade = {
  id: string;
  nome: string;
  email: string;
  avatar: string;
};

export type UsuarioBuscaComunidade = {
  id: string;
  nome: string;
  username: string;
  avatar: string;
};
